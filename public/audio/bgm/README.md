# 메뉴 배경음 넣는 곳

이 폴더에 `menu.mp3` 파일을 넣으면 메인, 팀, 선수, 리그, 경기 선택, 설정 화면에서
자동으로 반복 재생됩니다.

```text
public/
└── audio/
    └── bgm/
        └── menu.mp3
```

- 파일명과 확장자는 정확히 `menu.mp3`로 맞춰 주세요.
- 실제 경기 시작 시 BGM은 멈추고 관중석 소리로 전환됩니다.
- 설정의 `배경음` 스위치와 볼륨이 이 파일에 적용됩니다.
