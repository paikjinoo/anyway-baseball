/**
 * 온라인 대전 하루 보상 한도.
 *
 * 서로 짜고 번갈아 져 주며 보상을 찍어내는 담합은 이 총량 제한 하나로 막는다
 * (한 판을 이기든 열 판을 이기든 하루 총량이 같다). 같은 이유로 온라인 보상에는
 * 득점·완봉 보너스가 없다 — 점수에 값을 매기면 그게 곧 승부조작의 대가가 되기 때문이다.
 *
 * 저장소(firebase/store.ts)와 게임 로직(matchReward.ts) 양쪽이 참조하므로,
 * 순환 import를 만들지 않도록 상수만 따로 둔다.
 */

/** 온라인 대전으로 하루에 받을 수 있는 골드 상한. 승리 약 3경기분. */
export const ONLINE_DAILY_GOLD_CAP = 1200;

/** 온라인 대전으로 하루에 받을 수 있는 선수 경험치 총합 상한. */
export const ONLINE_DAILY_EXP_CAP = 2500;
